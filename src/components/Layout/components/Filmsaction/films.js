import classNames from 'classnames/bind'
import Styles from './films.module.scss'
const cx = classNames.bind(Styles)
function Films({ props }) {
    return <div className={cx('movie_banner_wrapper')}>
        
    </div>
}

export default Films
